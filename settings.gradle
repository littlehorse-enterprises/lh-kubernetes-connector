pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "${quarkusVersion}"
        id "com.diffplug.spotless" version "${spotlessVersion}"
    }
}
rootProject.name='lh-kubernetes-connector'
